// import { socketRtc, socket2D } from "../saga/socketSaga";

export const soketCharacterApi = {
  // enterRoom: (roomName, peerId) => {
  //   socket2D.emit("enterRoom", roomName, peerId);
  // },
};

export const soketVideoApi = {
  // enterRoom: (roomName, peerId) => {
  //   socketRtc.emit("enterRoom", roomName, peerId);
  // },
};
